#include <stdio.h>
void main(void){
	int a[2][5]={{1,2,3,4,5},{0,9,8,7,6}};
	int (*p)[5];  /* 声明p是一个指向长度为5的一维数组的指针 */
	p=a+1; 
/* 让p指向二维数组a中的第2个元素，即第2行（长度为5的一个一维数组）*/
	printf("%d\n",*(*p+2));  /* 通过p访问二维数组中的a[1][2] */
	printf("%d\n",(*p)[2]);   /* 通过p访问二维数组中的a[1][2] */
}
